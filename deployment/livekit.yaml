port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 60000
  # Note: In production, change this to your VPS public IP
  use_external_ip: false 
  udp_port: 7882
turn:
  enabled: true
  # Domain is required for valid TLS/TURN
  domain: localhost
  tls_port: 5349
  udp_port: 3478
  # This uses the same keys as LiveKit for TURN auth
logging:
  json: false
  level: info
